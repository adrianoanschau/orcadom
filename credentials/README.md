# Credenciais locais

Arquivos desta pasta **não entram no Git**.

O JSON de OAuth do Google Cloud (usado pelo n8n no fluxo de email) fica
só na máquina local. Baixe o client secret no console do Google e
coloque-o aqui — o nome típico é `client_secret_….apps.googleusercontent.com.json`.
